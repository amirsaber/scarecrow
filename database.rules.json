{
  "rules": {
    "Orders": {
      ".read": "true",
      ".indexOn": [
        "uid",
        "grain",
        "type"
      ],
      "$orderId": {
        ".write": "!data.exists() || data.child('uid').val() === auth.uid"
      }
    }
  }
}
